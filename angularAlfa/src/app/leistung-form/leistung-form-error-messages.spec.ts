import { LeistungFormErrorMessages } from './leistung-form-error-messages';

describe('LeistungFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new LeistungFormErrorMessages()).toBeTruthy();
  });
});
