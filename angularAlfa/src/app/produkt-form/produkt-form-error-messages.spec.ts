import { ProduktFormErrorMessages } from './produkt-form-error-messages';

describe('ProduktFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new ProduktFormErrorMessages()).toBeTruthy();
  });
});
